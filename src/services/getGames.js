
import {baseurl, CONFIG} from "../utils/baseurl";

 
 
const GAMES_ENDPOINT = baseurl('');

const customFetch = () => {
   
}

/* export const getAulas= async () => {
    try {
      const data = await axios.get(GAMES_ENDPOINT, CONFIG);
      console.log('Get');
      return data;
    } catch (err) {
      console.log(err);
    }
  }; 
    */
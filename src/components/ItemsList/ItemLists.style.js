import styled from 'styled-components';

export const ContainerList = styled.div`
    display: grid;
    justify-content:center;
    gap: 30px 20px;
    grid-template-columns: repeat(auto-fill, minmax(20rem, 18rem));
`;